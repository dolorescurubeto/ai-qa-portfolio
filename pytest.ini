[pytest]
testpaths = tests
pythonpath = week03-metrics
markers =
    slow: tests that load ML models (BERTScore, etc.)
    integration: full golden-set sweep
    regression: locked baseline counts — change only when data intentionally updates
    e2e: Playwright browser tests — run locally with pytest -m e2e

addopts = -ra -m "not e2e"
