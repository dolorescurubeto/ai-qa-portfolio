# Week 1 — Factual baseline

## Your only task today

```powershell
cd C:\Users\dell\ai-qa-portfolio\week01-baseline
python evaluate_factual.py
```

You do not need `venv` yet (this script uses only the standard library).

## What you will see

- 5 simulated chatbot responses
- Some PASS and some FAIL
- Why "similar" is not the same as "correct"

## Optional task (when you want)

1. Open `evaluate_factual.py`
2. Add a `cand6` with invented text
3. Run again and see if it passes or fails
